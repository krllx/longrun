== init (project) and a linked worktree
  ok   project init --hq
  ok   worktree link -> registry entry, nothing written into the worktree
  ok   where in the worktree resolves to the project
  ok   session state lives outside the project and the worktree
== shared notes: add / dedupe / budget / rm / replace / prune
  ok   own note without a session is refused (exit 3) and points at --shared
  ok   add --shared writes [n1] into the project NOTES.md
  ok   duplicate detected
  ok   bad tag rejected
  ok   budget refuses with exit 2 and candidates
  ok   NOTES.md never exceeds cap (759 B)
  ok   prune --auto --shared shrinks to <=60% (390 B) and archives
  ok   prune keeps dead/decision over fact
  ok   rm n1 archives
  ok   replace n2
  ok   add after prune fits
  ok   shared entries carry the author label
== ledger + ask + inbox
  ok   ledger in the project
  ok   ledger DUE shown
  ok   ledger done with outcome
  ok   ask --ledger -> ledger @user + inbox file
  ok   hand-edited ledger line survives a rewrite
  ok   hand-edited line is listed but not addressable as L0
  ok   report from stdin
  ok   inbox ls counts 2
  ok   inbox ack archives
== hooks with real payload shapes
  ok   SessionStart prints digest with shared notes, own notes, ledger and the commands
  ok   SessionStart exports env for Bash
  ok   SessionStart creates the session dir with meta + journal
  ok   session index records project and key
  ok   own note lands in the session dir as [s1], not in the project
  ok   own note has no author label
  ok   rm s2 removes an own note and archives it in the session dir
  ok   replace s1
  ok   notes shows both files
  ok   40 read-only commands are not activity: no card, edits counter stays 0
  ok   mid-turn card names the files it edited, once per window (call 46 yes, 47 no)
  ok   UserPromptSubmit right after a card: ledger items yes, a second card no
  ok   the card of the turn that ended names its file and its failed command, not a bare counter
  ok   and the next turn is quiet: a rate limit, not a card after every turn
  ok   a turn of pure reading gets no card, and a note write resets the counters
  ok   a failed command stepping over the window boundary does not swallow the card
  ok   PostToolUseFailure -> FAIL line in journal
  ok   PreCompact prints the summariser instructions on stdout (this is what Claude Code appends to the compaction prompt)
  ok   the hint names the notes that come back by themselves, and is plain text (JSON stdout would be read as a decision object)
  ok   the hint stays inside compact_hint_max_bytes
  ok   the compact-hint command previews exactly what the hook sends
  ok   compact-hint --clear drops the session line
  ok   compact_hint false -> the hook prints nothing and the compaction runs unsteered
  ok   PreCompact snapshot has user msgs + edited files + FAIL
  ok   the hint is journalled with its size, so it is visible after the fact
  ok   PostCompact archives the summary verbatim under the project archive, named by session
  ok   SessionStart(compact) digest: own notes + journal tail + archive pointer
  ok   SessionStart(compact) digest carries the mechanical HANDOFF (failure + framing + files)
  ok   HANDOFF skips one-word replies and carries the /compact instructions as FOCUS
  ok   a failed command alone is worth a card, with no edits in the turn at all
  ok   handoff appears only after a compaction, not on plain startup
  ok   compactions counted
  ok   PostToolUse with the shell cwd outside the project still counts in the session's dir
  ok   SessionStart(compact) with the shell cwd elsewhere injects THIS session's shared + own notes
  ok   hooks ignore an inherited LONGRUN_DIR and bind to their own cwd's project
  ok   the compaction that hit while the shell was elsewhere is counted for this session
  ok   Stop stores last status
  ok   SessionEnd marks ended
== recall: notes, journal, archived summary, transcript
  ok   recall finds archived summary + transcript
  ok   recall finds the own note first
  ok   recall finds FAIL line in journal
  ok   recall miss exits 1
== digest size cap
  ok   digest respects inject_max_bytes (1478 B <= 1500)
  ok   all budgets full: digest fits (8836 B <= 9000) and keeps the @user/DUE rows
  ok   all budgets full: neither notes file is what gives way
  ok   over budget: the digest sheds the OLDEST entries and keeps the newest (1307 B <= 1400)
  ok   over budget: a pin is never the entry that gives way
  ok   PRUNE NEEDED is measured against what a digest can carry, not against the file cap
  ok   config refuses notes budgets that cannot fit inject_max_bytes (exit 2)
  ok   ...and accepts them once inject_max_bytes has room
== recall ranking: curated notes are not crowded out by a noisy archive
  ok   recall shows the NOTES.md hit despite 15 noisier archive hits
== status and concurrency
  ok   status lists the session with its state, own-note count and NEEDS USER
  ok   status hides a session with no tool calls and no turns (app noise), --all shows it
  ok   20 parallel shared adds, none lost (20)
  ok   ids unique under concurrency
  ok   20 parallel own adds, none lost (20)
== gc retention
  ok   gc archives expired shared and own notes and deletes old archive files
  ok   archive/notes.md ages line by line: the old line is gone, recent lines stay
  ok   gc archives a session silent for a week into archive/sessions/<key>/
== older layouts are migrated, not orphaned
  ok   a running session's first hook after the upgrade migrates its flat files first (counters continue: 51)
  ok   flat files meeting an existing new-layout dir are merged: counters summed, old journal lines first
  ok   gc moves 0.1 heartbeats out of the project dir and 0.2 flat files into per-session dirs
  ok   migrated history stays visible to status and recall
  ok   a 0.2 worktree store resolves to its hub as the project
  ok   the notes it held are parked in the project archive and recall finds them
== notes-only mode (the HQ layer is opt-in)
  ok   plain init creates no ledger and no inbox
  ok   where reports notes-only
  ok   shared notes/recall work in notes-only mode
  ok   HQ command refused with exit 4 and a way out
  ok   digest has no LEDGER block in notes-only mode
  ok   a worktree of a notes-only project is notes-only too (no ledger/report hints)
  ok   a hand-written heading in NOTES.md is kept, not silently dropped
  ok   --hq turns the layer on afterwards
== gc auto-prunes cheap entries so writing never simply stops
  ok   gc auto-archived cheap todo entries (797 -> 488 B)
  ok   gc never auto-archives a dead end
== recall reaches earlier sessions' transcripts and other sessions' own notes
  ok   recall finds a detail from a previous session's transcript
  ok   recall reports how many transcripts it scanned
  ok   recall reaches the current session's subagent transcripts
  ok   recall reaches other sessions' own notes
== send: socket to a live peer, inbox to a stopped one, delivery by the target's hooks
  ok   send --list shows the running peer with its socket
  ok   --resume is refused for a running session
  ok   send to a running peer goes over its socket as a user message with a sender stamp
  ok   send to a session that is not running queues a file in the project inbox
  ok   the target's UserPromptSubmit delivers the message once and archives it
  ok   the target's PostToolUse delivers a message between tool calls as additionalContext
  ok   a message longer than the injection limit says it was cut and where the whole text is
  ok   the target's SessionStart digest carries a queued message
  ok   --resume runs claude -p <text> --resume <sid> in the session's cwd with LONGRUN_* scrubbed
  ok   a message from another project lands where the TARGET's hooks look (its own project inbox)
  ok   send resolves a desktop sidebar title (archived twin ignored)
  ok   send --list shows desktop titles
== watch: deferred checks, timer-free tick, delivery to a stopped and a running session
  ok   watch test: at +1h is not yet (rc 1)
  ok   watch test: missing file is not yet
  ok   watch test: cmd exit 3 is a hard error
  ok   watch add refuses a check that fails hard
  ok   watch add with an already-true check registers nothing
  ok   watch add by sidebar title -> w1, inbox delivery announced
  ok   watch add snapshots PATH for the timer
  ok   watch ls shows it pending
  ok   tick with the condition false keeps it pending
  ok   tick fires: message queued in the TARGET's inbox with the --then text
  ok   fired watch logged in the target's journal
  ok   the target's next SessionStart delivers the fired watch
  ok   watch ls hides fired, --all shows it with delivery
  ok   watch follows the desktop entry to the new CLI id (rename + resume)
  ok   the resumed session (new CLI id) receives the message addressed to its desktop id
  ok   the resumed session continues the prior's dir (priorCliSessionIds): same key, journal continues, no second dir
  ok   three hard errors -> broken, reported to the registering session
  ok   expired watch is reported and closed
  ok   fired watch reaches a running session over its socket as a user turn
  ok   unknown target refused
  ok   watch rm cancels
  ok   unwritable project inbox -> message spooled in the session dir, watch marked fired, tick exit 0
  ok   the target's hooks read the session spool too and archive the message
  ok   digest lists pending watches for this session
== env exports are shell-quoted
  ok   a path with a space survives the env file
== inert outside a project
  ok   hook inert when no longrun project
  ok   cli refuses without a project (exit 3)
  ok   digest outside a project: exit 0, digest-shaped, names init/link/onboard
  ok   where outside a project still exits 1, so a script can branch on it
  ok   the skill body never lets those two fail the prompt
== config command
  ok   config set writes the project config.json
  ok   config shows the value with its source
  ok   config set --global writes the global file and coerces the bool
  ok   unknown key refused (exit 2)
  ok   non-integer refused (exit 2)
  ok   config unset removes the key
  ok   note_max_chars comes from the config (exit 2)
  ok   watch_ttl_days sets the default --for (under 3 days instead of 7)
  ok   autocompact_window accepts 300k and stores tokens
  ok   autocompact_window refuses junk (exit 2)
  ok   pr_tool refuses unknown tools (exit 2)
== onboarding and the PR tool
  ok   onboard prints the brief with state and settings
  ok   onboard reports the autocompact mismatch with Claude Code
  ok   digest suggests onboarding until it is done
  ok   onboard done records the date and silences the hint
  ok   pr-merged registered in a git repo picks gh, in an Arcadia checkout picks arc, discarded == closed
  ok   pr_tool in the config overrides the folder
== the platform layer: which timer, which dialog, where the credentials are
  ok   cron_spec turns seconds into a crontab schedule (and never goes below a minute)
  ok   cron_strip removes our two lines and leaves the rest of the crontab alone
  ok   the systemd units name the script, the interval and a log, and escape % for systemd
  ok   watch_timer in the config pins the scheduler on any OS
  ok   watch install with watch_timer=none explains what to do instead (exit 1)
  ok   watch_timer refuses an unknown scheduler (exit 2)
  ok   auto picks launchd on macOS and systemd or cron on Linux
  ok   the desktop metadata dir follows the OS (Library on macOS, XDG on Linux)
  ok   the dialog program is chosen by OS: osascript on macOS, zenity/kdialog on a Linux desktop, none headless
  ok   a machine with no dialog says why (headless, or nothing installed)
  ok   with no dialog program the ask fails with a reason instead of hanging
== notifications (terminal-notifier over osascript: macOS files an osascript notification and draws nothing)
  ok   auto prefers terminal-notifier whenever it is installed
  ok   without it auto falls back to the OS default: osascript on macOS, notify-send on a Linux desktop
  ok   a headless box with nothing installed resolves to none, not to a command that will fail
  ok   the notifier config key pins the program and refuses an unknown one (exit 2)
  ok   notify --test with notifier=none exits 1 and names the config key, not a missing binary
  ok   each notifier gets its own argv, and only terminal-notifier is asked for a sound
  ok   LONGRUN_NO_UI sends nothing and says that is why (exit 1)
  ok   notify_user reports the program it used, so a caller can log how it notified
  ok   the presented flag is unreadable without the macOS database, and that is not a failure
  ok   no macOS database means no records, not a crash
  ok   a click target is built only from a real app session id (local_<uuid>), never from a CLI id
  ok   a missing notifier is never an error: notify_user returns a string for every kind and raises nothing
  ok   notifications go through our own sender bundle as soon as it exists, and fall back to PATH when it does not
  ok   the bundle keeps terminal-notifier's own id prefix, so the copy is recognisably a copy
  ok   notify setup refuses politely off macOS instead of building a broken bundle
  ok   notify setup with a missing icon file is an argument error, and nothing is built
  ok   notify_turn_end is off by default, and refuses anything but off/unfocused/always (exit 2)
  ok   unfocused notifies only while the Claude app is not the one in front
  ok   always ignores the front app, off never fires, and an unknown value is treated as off
  ok   the front app is read without Automation permission (lsappinfo), and never crashes when it cannot be told
  ok   only terminal-notifier carries the click target; the other two drop it instead of failing
== important sessions (the turn end the user must not miss)
  ok   a session is not important by default, and says which setting turn ends follow instead
  ok   important next arms exactly one turn end
  ok   the flag is in the digest head, so it survives a compaction
  ok   status carries it as a session flag
  ok   one turn end spends the counter and clears the flag
  ok   the journal records that the turn end was notified (here: that this box has no notifier)
  ok   important N counts down one turn end at a time
  ok   important on outlives any number of turn ends
  ok   another session can be flagged by name, and is listed as important from anywhere in the project
  ok   important off is the only thing that drops it
  ok   an unknown argument is refused (exit 2), not silently taken as on
  ok   the flag beats both gates: notify_turn_end off and the Claude app in front
  ok   consume_important spends a counted flag and never goes below zero
  ok   the token comes from Claude Code's own credentials file when there is no keychain item
  ok   no credentials anywhere is an error, never an empty token

PASS=210 FAIL=0  (tmp: /tmp/longrun-test.lq49cd)
