[English](README.md) | Русский | [简体中文](README.zh-CN.md) | [日本語](README.ja.md) | [한국어](README.ko.md)

<p align="center">
  <img src="assets/hero.jpg" alt="longrun: память и координация сессий Claude Code" width="820">
</p>

<h1 align="center">longrun</h1>

<p align="center">
  Память и координация сессий Claude Code.<br>
  Проект держит свое состояние на диске, а пишут и разбирают его сами агенты.<br>
  Каждая сессия видит, что известно проекту и кто еще над ним работает.<br>
  Работа уходит той сессии, у которой есть контекст; ожидание надежно и ничего не стоит.<br>
  Одна сессия может вести остальные - и достучаться до вас, когда решить можете только вы.
</p>

<p align="center">
  <a href="#быстрый-старт">Быстрый старт</a> ·
  <a href="#четыре-способа-координации">Четыре способа координации</a> ·
  <a href="#как-это-работает">Как это работает</a> ·
  <a href="#команды">Команды</a> ·
  <a href="docs/REFERENCE.ru.md">Справочник</a> ·
  <a href="https://krllx.github.io/longrun/course/ru/">Курс</a>
</p>

<p align="center">
  <img alt="Claude Code" src="https://img.shields.io/badge/Claude_Code-skill-d97757">
  <img alt="Python" src="https://img.shields.io/badge/python-3.9%2B%2C_no_deps-3776ab">
  <img alt="macOS and Linux" src="https://img.shields.io/badge/macOS-launchd-000000">
  <img alt="Linux" src="https://img.shields.io/badge/Linux-systemd_%2F_cron-e95420">
  <img alt="Tests" src="https://img.shields.io/badge/tests-326_checks%2C_no_API_calls-2ea44f">
  <img alt="License" src="https://img.shields.io/badge/license-MIT-blue">
</p>

<p align="center">
  <a href="https://krllx.github.io/longrun/course/ru/"><img src="assets/course-banner.ru.svg" alt="Курс: как устроен longrun - одиннадцать коротких частей, около 15 минут" width="820"></a>
</p>

---

> Английская версия - источник, и она может быть впереди этой страницы. Правки приветствуются - см. [docs/TRANSLATION.md](docs/TRANSLATION.md).

## Быстрый старт

```bash
curl -fsSL https://krllx.github.io/longrun/install.sh | bash
```

> [!NOTE]
> Установщик добавляет скилл с командой `longrun` для терминала, хуки в `~/.claude/settings.json` и фоновый таймер, который срабатывает раз в пять минут. Спросит про установку уведомлений. `install.sh --uninstall` убирает все, что добавил.

<details>
<summary>Что именно происходит на машине</summary>

- `~/.claude/settings.json` - 11 хуков и два правила в permissions, которые разрешают агенту вызывать `longrun`. Файл сначала копируется в `~/.claude/backups/`, хуки, добавленные не longrun, не трогаются.
- `~/.claude/skills/longrun/` и симлинк `~/.local/bin/longrun`.
- MCP-сервер `longrun` в user scope (`claude mcp add`) - он дает агенту инструменты `ask` и `notify`.
- **фоновый таймер** раз в пять минут: агент launchd на macOS, systemd user timer или строка в cron на Linux. Он проверяет заведенные watch и состояние сессий. Это несколько shell-проверок без модели и без токенов; сессию он будит, только когда ваше условие выполнилось. Флаг `--no-timer` отключает таймер.
- **уведомления на рабочем столе**, если ответить да на вопрос установщика: на macOS `brew install terminal-notifier`, если его нет, и одно тестовое уведомление, чтобы macOS запросила разрешение. На Linux только проверяется наличие `notify-send`. Флаги `--notify` и `--no-notify` задают ответ заранее.

Нужны macOS или Linux, Claude Code и python3 3.9+. Из клонированного репозитория запускается тот же [`install.sh`](install.sh).

</details>

Далее нужно открыть проект в Claude Code (новый или уже существующий) и сказать **"настрой longrun"**. Или из терминала:

```bash
cd ~/my-project && longrun init && claude "настрой longrun"
```

После этого команды не нужны, достаточно сказать: *"запиши"*, *"что мы уже пробовали"*, *"скажи, когда PR вольется"*.

> **Устройство скилла и примеры:** [курс](https://krllx.github.io/longrun/course/ru/) - одиннадцать коротких частей, около 15 минут.

<details>
<summary><b>Уведомления на рабочем столе</b> - зачем они и что спрашивает установщик</summary>

- **Зачем нужны:** сессия и так узнает о halt, сработавшем watch и законченном ходе; баннер нужен, чтобы *вы* заметили это, пока смотрите в другое окно. Для работы скилла они не нужны.
- **macOS:** `terminal-notifier` через Homebrew (встроенного способа показать баннер у macOS нет) и один запрос разрешения. Если при установке ответили нет, позже `brew install terminal-notifier`.
- **Linux:** `notify-send` (`libnotify`), в большинстве окружений рабочего стола он уже есть.

`longrun notify --test` проверяет, что уведомление действительно доходит до экрана. Две настройки, на которые стоит взглянуть: `notify_turn_end unfocused` (баннер, когда сессия закончила ход, а окно Claude не в фокусе) и `longrun important on|next` для той сессии, которую нельзя пропустить. Инструмент `notify`, которым пользуется агент, предоставляет MCP-сервер `longrun`; его регистрирует установщик. Подробности и особенности macOS: [docs/REFERENCE.ru.md](docs/REFERENCE.ru.md).

</details>

## Коротко

**Работает само, с первой сессии:** проект держит на диске набор заметок, которые агенты сами пишут, перечитывают и чистят, - тупики, решения, факты. Каждая сессия стартует, уже зная, что в них, какие еще есть сессии и чем каждая занималась в последний раз, а на каждом ходу видно, что другие изменили с тех пор.

**По просьбе или когда агент сам видит необходимость:** работа уходит той сессии, у которой уже есть контекст; "скажи, когда PR вольется" превращается в watch, который не тратит токены на ожидание и который нельзя проспать; уведомление доходит до вас о той сессии, которую нельзя пропустить; одна сессия координирует остальные и ведет их к заданной цели, а когда разблокировать можете только вы, ставит перед вами диалог.

**И попутно:** заметки на диске не деградируют, поэтому хуки возвращают их после каждой компакции, `/clear` и resume. Эта часть значит теперь меньше, чем раньше: те же хуки говорят суммаризатору, что сохранить, так что и обычная компакция теряет меньше, чем теряла.

## Зачем

| Без longrun | С longrun |
|---|---|
| Все, до чего сессия додумалась, живет внутри одного разговора. Следующая сессия (завтрашняя или та, что в соседнем окне) стартует с нуля и спрашивает у вас, что происходит. | **Проект, который помнит.** Один `.longrun/` на проект: заметки, которые агенты сами пишут, перечитывают и чистят. Каждая сессия стартует с ними, а еще с тем, кто еще работает и чем каждый занимался в последний раз. |
| Вторая сессия не знает о первой. Одна открыла PR, другая говорит "PR еще не создан". | **Сообщения и задачи.** Работа уходит той сессии, у которой уже есть контекст, и приходит туда ходом пользователя. Остановленной тоже: сообщение ждет ее в inbox проекта. |
| "Скажи, когда PR вольется" превращается либо в цикл опроса, который тратит токены, либо в паузу на заданное время (sleep), после которой событие уже давно произошло или еще не наступило. | **Watch.** Фоновый таймер проверяет условие раз в пять минут без модели и будит сессию, когда оно выполнилось. Надежно, реактивно, бесплатно. |
| Пять сессий на одном проекте, и только вы знаете, что сделано, что застряло и что дальше, - и только вы замечаете, когда одна из них ждет вас. | **Оркестратор.** Одна сессия ведет доску для остальных, замечает застрявшую сессию и ставит диалог поверх всех окон, когда решить можете только вы. |
| Компакция сжимает историю в резюме, и первыми пропадают причины решений: почему подход отбросили, почему выбрали этот путь. | **Заметки на диске не деградируют,** а хуки возвращают их после нее. Они же говорят суммаризатору, что сохранить, так что и сама компакция теряет меньше. |

Один python-файл, без зависимостей.

## Часть этого Claude Code уже умеет

longrun - про то, что переживает ход и сессию. Сначала стоит взять встроенное:

- **Одна сессия идет к одному проверяемому условию** -> `/goal`: он держит эту сессию, пока отдельный оценщик не подтвердит, что условие выполнено. У longrun есть доска и уговоры, а оценщика нет.
- **Разделить работу внутри одного хода** -> сабагенты и workflow: они работают параллельно, результат сводится вместе, и все это пропадает вместе с ходом.
- **То, что переживет саму задачу** (кто вы, как вы работаете, постоянные соглашения) -> автопамять Claude Code.
- **Сообщение сессии, которая работает прямо сейчас** -> встроенный `SendMessage`; `ListAgents` перечисляет то, что запущено.
- **Несколько окон, часами и днями** -> longrun: состояние, которое переживает любой ход, остановленная сессия, которой все равно можно написать, ожидание без токенов и одна сессия, координирующая остальные.

## Четыре способа координации

> [!NOTE]
> Команды ниже - это то, что агенты выполняют сами: скилл говорит им, когда записать заметку, когда отправить сообщение, когда завести watch. Заучивать тут нечего: вы говорите *"запиши"* или *"скажи, когда PR вольется"*, а чаще не говорите ничего. Они приведены потому, что любую из них при желании можно выполнить руками.

### 1. Общие документы на диске

Один `.longrun/` на проект хранит заметки, которые видят все сессии, - именно туда заметка уходит по умолчанию. Оставить что-то только себе сессия может флагом `--own`, вне дерева репозитория. Хуки возвращают и те и другие после каждой компакции, `/clear` и resume, а на каждом ходу показывают, что изменили другие сессии.

```bash
longrun add -t pin "PR 42 = branch feature/checkout"           # общая: видят все сессии
longrun add --own -t ctx "только оформление заказа, не корзина"  # только этот разговор
longrun doc add research/plan.md "план выкатки и что открыто"
longrun recall 429                                             # заметки, эти файлы, журналы, транскрипты
```

### 2. Делегирование той сессии, у которой есть контекст

Сообщение другой сессии приходит как ход пользователя. Работающая сессия получает его сразу через свой сокет. Остановленная заберет его из inbox проекта на следующем ходу; разбудить ее сразу можно флагом `--resume`, но это запуск `claude -p` в фоне, а значит, токены. Сессия адресуется по имени, которое вы видите в сайдбаре.

Половину этого Claude Code умеет сам: `SendMessage` пишет сессии, которая **работает**, а `ListAgents` перечисляет работающие. `longrun send` добавляет остальное: остановленную сессию (сообщение ждет в inbox), адресацию по заголовку из сайдбара вместо id сессии, `--resume`, чтобы поднять сессию headless, и тот же путь, которым пользуются сработавший watch и доска, а у них нет модели, которая вызвала бы инструмент за них.

```bash
longrun send "PR 43: платежи" "PR 42 влит, перебазируйся на main"
longrun board assign T8 "PR 43: платежи"      # задача вместо сообщения
```

### 3. По событию: ждать, не тратя токены

Watch - это детерминированная проверка, которую таймер запускает раз в пять минут без участия модели (launchd на macOS, systemd-таймер пользователя или cron на Linux). Когда условие выполнилось, сессия получает текст из `--then` сообщением. Если ноутбук спит, проверка просто откладывается.

```bash
longrun watch add --to "PR 43: платежи" --then "перебазируйся на main" -- pr-merged 42
longrun watch add --then "проверь выкатку" -- at 10:00
```

Проверки: `pr-merged` (GitHub через `gh`), `pr-status`, `at`, `file`, `http`, `cmd`.

### 4. Одна сессия координирует остальные - и достучится до вас, когда придется

Одна сессия берет роль оркестратора и ведет доску: цель, задачи, факты извне. Рабочие сессии берут, закрывают и помечают задачи как заблокированные. Каждое изменение на доске будит оркестратора: он раздает следующую задачу, разбирает блокировки или спрашивает человека диалогом поверх всех окон. Он не опрашивает сессии и сам их не запускает: в приложении он оставляет чип, который открывает сессию по клику, а в терминале выдает первую строку, которую нужно вставить в новую сессию.

Это координация нескольких окон плюс линия связи с вами, а не самостоятельность: вести **одну** сессию к условию, которое может проверить оценщик, - задача `/goal`.

```bash
longrun orchestrate start --goal "довести корзину до релиза"   # в координирующей сессии
longrun board take T7; longrun board done T7 "PR 42 влит"      # в рабочей
longrun board add --fact "ревьюер просит переименовать поле"   # узнали что-то снаружи
longrun ask "Мержим PR 42?" --options "Да,Нет"                 # диалог, ответ приходит сразу
```

Управление остается у вас. Оркестратор и смотритель только предлагают: они могут написать сессии, поставить вопрос перед вами и остановить все сессии разом командой `longrun halt`, а снять эту остановку можете только вы. Работающий инструмент никто не убивает: зависшее окно останавливаете вы сами, клавишей Esc.

## Как это работает

```mermaid
sequenceDiagram
    participant S as сессия Claude Code
    participant L as longrun (хуки)
    participant D as заметки на диске
    S->>L: сессия стартует
    L->>D: прочитать общие заметки проекта и свои заметки этой сессии
    L-->>S: дайджест: что известно, кто еще работает, что ждет
    Note over S: агент работает, пишет по строке на тупик или решение
    S->>D: longrun add ...
    S->>L: каждый ход
    L-->>S: что изменили другие сессии, сообщения для вас
    S->>L: скоро компакция
    L->>D: снимок и указание суммаризатору, что сохранить
    S->>L: сессия стартует снова (после компакции, /clear, resume)
    L-->>S: тот же дайджест плюс место, где остановились
```

**Старт.** Хук находит проект по каталогу и печатает дайджест: общие заметки, свои заметки, другие сессии (активны ли, чем занимались в последний раз), ожидающие watch, недоставленные сообщения.

**Работа.** Агент пишет по строке на тупик, решение или добытый факт. Хуки считают правки и пишут упавшие команды в журнал. После долгой серии правок без заметки - одно напоминание агенту записать ее.

**Каждый ход.** Доставляются сообщения из inbox. Изменения общих заметок другими сессиями приходят в виде дельты: `+` добавлено, `~` переписано, `-` удалено.

**Компакция.** До нее - снимок последних просьб, правленых файлов и недавних падений, плюс инструкции суммаризатору: сохранить тупики с причинами, сохранить точные строки, выбросить то, что можно снова получить одним чтением файла. Промпт резюме у автокомпакции и у `/compact <текст>` один и тот же, так что это тот же способ управлять резюме, только автоматический: не нужно ловить момент перед компакцией. После компакции резюме дословно сохраняется в архив. Следующий старт печатает дайджест и блок HANDOFF, и сессия продолжает с того же места. Resume и `/clear` продолжают те же заметки, форк получает копию.

**Уборка.** Раз в час: старые записи в архив (кроме `pin`), журналы обрезаются, молчавшие сессии архивируются. Команда `add` отказывает, когда лимит заметок исчерпан, и подсказывает, что можно удалить. Ничего не удаляется молча.

## Три сущности

| Сущность | Что это | Что хранит |
|---|---|---|
| **Проект** | Один каталог `.longrun/`, в папке проекта или вне дерева (`--external`) | Общие заметки, inbox, доска, архив |
| **Сессия** | Один разговор Claude Code; в приложении - строка в сайдбаре. Resume выдает новый id, longrun связывает старый id с новым | Свои заметки, журнал, счетчики, последний статус |
| **Каталог** | Папка, где сессия стартовала: корень репозитория, worktree, подкаталог | Ничего. Только говорит, к какому проекту относится сессия |

Worktree привязывается командой `longrun link <проект>` и своего хранилища заметок не имеет.

## Что писать

Критерий один: **можно ли получить это заново одной командой, чтением файла или grep?** Если да, записывать не нужно. Не влезает в одну строку? Значит, это файл, а заметки несут указатель на него: `longrun doc add research/plan.md "план выкатки и что еще открыто"`. Указатель видит каждая сессия на каждом старте и открывает файл, только когда он ей нужен.

| Тег | Что | Куда |
|---|---|---|
| `dead` | подход не сработал, и почему | свои; общие, если в тот же тупик может зайти другая сессия |
| `decision` | выбор и причина | общие, если касается других |
| `fact` | факт об окружении, который дорого достался | общие |
| `pin` | факт без срока давности: номер PR, ветка, хост | общие |
| `ctx` | рамки задачи от человека | свои |
| `doc` | указатель на файл, который слишком длинный для заметки: `longrun doc add <путь> "что внутри"` | общие |

Заметка, которая перестала быть правдой, не удаляется у всех за спиной: `longrun stale n12 "стейджинг переехал на vla-07"` помечает ее, пометку видит каждая сессия, а ближайшая уборка возьмет помеченные записи первыми. `longrun mute n12` просто убирает запись из *вашего* дайджеста и ни для кого другого ничего не меняет.

Журнал ведет механическую запись сам - что правили, что упало, когда была компакция, - поэтому пересказывать в заметках ход работ не нужно. То, что переживет задачу (кто пользователь, как он работает), - в автопамять Claude Code, а не в longrun.

## Команды

```bash
longrun init [--external] | link <проект> | where | onboard | config
longrun add [--own] -t TAG "..." | rm | replace | stale | mute | notes | prune | recall <слово>
longrun doc add <путь> "что внутри" | doc ls | doc touch n12 "..." 
longrun status | send [--list] [--resume] КТО "..."
longrun watch add --to КТО --then "..." -- pr-merged 42 | at 10:00 | cmd '...' | file /путь | http URL
longrun orchestrate start --goal "..." | board [add --fact|ack] | ask | halt | resume
```

Все флаги, форматы файлов и проверенные факты о Claude Code: [docs/REFERENCE.ru.md](docs/REFERENCE.ru.md). Устройство оркестратора и что осталось: [docs/ORCHESTRATOR.ru.md](docs/ORCHESTRATOR.ru.md).

<!-- сайт: нюансы, границы и особые случаи переезжают на сайт; ссылку поставить здесь -->

<details>
<summary><b>Частые вопросы</b></summary>

**Агент говорит "PR еще не создан", хотя он есть.** Этого факта нет в общих заметках, добавьте его: `longrun add -t pin "PR 42 = branch feature/checkout"`.

**Сессия в worktree не видит заметок проекта.** `longrun where` должен показать проект. Если пишет "not initialised": `longrun link <проект>` из этого worktree.

**Сообщение отправлено, а сессия молчит.** `longrun send --list`: если адресат в статусе stopped, сообщение лежит в inbox и придет на следующем ходу. Нужно доставить сейчас - добавьте `--resume`.

**Watch не срабатывает.** `longrun watch status`, `longrun watch ls --all`, `longrun watch test -- <та же проверка>`. Частая причина - относительный путь или alias в `cmd`.
</details>

## Разработка

```bash
bash tests/run.sh            # 211 регрессионных проверок, без API
bash tests/scenarios.sh      # 37 сценариев, по одному на задачу
bash tests/orchestrator.sh   # 51: слой оркестратора
bash tests/ask.sh            # 27: диалог и MCP-сервер
```

Установщик копирует файлы в `~/.claude/skills/longrun/`; правки в репозитории сами по себе не применяются. Уже запущенная сессия подхватывает обновление без перезапуска, потому что хук - отдельный процесс на каждое событие.

## Лицензия

[MIT](LICENSE)
