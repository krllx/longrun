[English](REFERENCE.md) | Русский

> Справочник: все команды и флаги, форматы файлов, хуки, бюджеты, проверенные факты. Как это устроено и зачем - в [README.ru.md](../README.ru.md).

# longrun - справочник

Версия 0.6.1. Проверено на Claude Code 2.1.238 (CLI) и 2.1.260 (desktop) на macOS; линуксовая половина слоя платформы (таймеры systemd и cron, выбор программ диалога и уведомления) проверена на Ubuntu 24.04 с python3 3.12, там же зеленые все четыре набора тестов. Раздел 12. Факты о хуках и приложении сняты с живых запусков и сверены с официальными доками; сырье в [research/VERIFIED.md](../research/VERIFIED.md) и [research/hook-payloads/](../research/hook-payloads/).

## 1. Сущности

- **Проект** - каталог `.longrun/`. Общая память всех сессий, которые стартовали внутри проекта или в каталоге, привязанном к нему. Имя проекта - имя родительской папки (`shop`), либо ключ `name` в `config.json`.
- **Сессия** - один разговор Claude Code. Ключ сессии - первые 8 символов ее первого CLI id. Принадлежит одному проекту: связь записывается на `SessionStart` в `~/.claude/longrun/sessions/_index/<CLI id>.json` и дальше все хуки идут по ней, а не по cwd (cwd в payload хука - это cwd шелла после `cd`).
- **Каталог** - где сессия стартовала. Определяет проект: вверх по дереву до `.longrun/` (каталог или файл-указатель с путем в первой строке), иначе `~/.claude/longrun/registry.json` по самому длинному совпавшему префиксу. Каталог ничего не хранит.

### Как разрешается идентичность сессии

| Событие | Что делает longrun |
|---|---|
| `SessionStart` с известным CLI id (компакция, resume из CLI, `/clear` без смены id) | тот же ключ, что в `_index` |
| Новый CLI id, у которого в файле приложения есть `priorCliSessionIds` (resume в приложении; приходит как `source=startup`) | ключ предыдущего id из цепочки; все, что успело записаться под новым id (например, журнальная строка от `watch`), сливается в каталог цепочки |
| Новый CLI id с `source=clear` | ключ сессии, которая закончилась с причиной `clear` в том же cwd не позднее трех минут назад |
| Новый CLI id, у которого приложение еще не записало метаданные | пока свой ключ; на каждом ходу хук перечитывает файлы приложения, и как только цепочка видна, перепривязывает сессию и один раз показывает ее свои заметки с пометкой "continued from CLI id ..." |
| `source=fork`, в файле приложения есть `forkedFromSessionId` | свой ключ, свои заметки родителя копируются один раз |

Файлы приложения: `~/Library/Application Support/Claude/claude-code-sessions/*/*/local_*.json` (macOS; на Linux `$XDG_CONFIG_HOME/Claude/...`, `LONGRUN_DESKTOP_DIR` переопределяет оба), поля `sessionId` (стабильный id записи в сайдбаре), `cliSessionId`, `priorCliSessionIds`, `forkedFromSessionId`, `title`, `cwd`, `isArchived`, `lastActivityAt`. На 07.09.2026 у 193 из 210 записей `cliSessionId` не совпадает с `sessionId`, а цепочка `priorCliSessionIds` есть у пяти: id приложения - его собственный, а не бывший CLI id.

## 2. Файлы и форматы

### Проект `<root>/.longrun/` (или `~/.claude/longrun/projects/<slug>/` при `--external`)

| Файл | Формат |
|---|---|
| `NOTES.md` | заголовок и строки `- [n<id>] MM-DD <tag> (<автор>): <текст>`. Автор - заголовок сессии из сайдбара (до 24 символов), иначе имя каталога. Чужие строки (ручные правки, `## заголовки`) сохраняются как записи без id |
| `inbox/*.md` (и `inbox/` в каталоге сессии, см. watch) | `<ts>-msg-to-<адрес>-from-<sid8>.md` (сообщения сессиям; адрес - `d<8 символов id приложения>` или `sid8`), `<ts>-report-<scope>-<sid8>.md`, `<ts>-ask-...`. Доставленные и подтвержденные уезжают в `inbox/.archive/` |
| `config.json` | переопределения настроек проекта, см. раздел 6 |
| `state.json` | `next_note`, `last_gc` |
| `stale.json` | id -> `{why, at, by}` для записей, которые сессия пометила как переставшие быть правдой |
| `archive/compact/<ключ сессии>-<ts>.md` | резюме компакции дословно, включая блок `<analysis>`, которого нет в транскрипте |
| `archive/precompact/<ключ>-<ts>.md` | снимок перед компакцией: текст `/compact ...`, последние просьбы пользователя длиннее 40 символов, правленые файлы, FAIL-строки |
| `archive/notes.md` | удаленные, замененные и состарившиеся записи, по строке с полной датой; строки с пометкой `legacy <путь>` - заметки хранилищ worktree версии 0.2 |
| `archive/sessions/` | `<ключ>.journal.md` (обрезанные хвосты журналов) и `<ключ>/` (каталоги сессий, молчавших дольше `session_dead_days`) |

### Сессия `~/.claude/longrun/sessions/<ключ проекта>/<ключ сессии>/`

Ключ проекта - путь каталога проекта с не-буквенными символами, замененными на `-` (последние 90 символов).

| Файл | Формат |
|---|---|
| `notes.md` | как `NOTES.md`, но `[s<id>]` и без автора |
| `journal.md` | `MM-DD HH:MM <строка>`, пишут хуки и команды, которые что-то меняют: `session <source>`, `FAIL \`<команда>\` -> Exit code N ...`, `compaction ...`, `summary archived ...`, `send -> ...`, `watch wN ...`, `session end (<reason>)` |
| `meta.json` | `sid` (текущий CLI id), `skey`, `cwd`, `scope`, `started`, `last_seen`, `ended`, `tools`, `turns`, `edit_tools_since_note`, `fails`, `files` (правленые), `compactions`, `last_status` (первые 200 символов последнего ответа, хук `Stop`), `permission_mode`, `desktop_id`, `shared_seen` (id -> хеш текста и пометки stale общих записей, которые сессия уже видела), `muted`, `swept_at`, `sessions_seen` (skey -> состояние и последний ответ, о которых уже сообщили), `last_note`, `turn_files`/`turn_fails`/`turn_card` (что изменили текущий и прошлый ход, для карточки хода), `carded_at_turns`/`carded_at_tools` |
| `state.json`, `archive/notes.md` | счетчик id своих заметок; удаленные свои записи |

### Глобальное `~/.claude/longrun/`

`registry.json` (`links`: каталог -> каталог проекта), `config.json` (глобальные переопределения), `sessions/_index/<CLI id>.json` (`local`, `cwd`, `skey`, `at`), `watch/` (`w<N>.json`, `env.json` со снимком PATH, `state.json`, `run.log` и лог самого планировщика: `launchd.log`, `systemd.log` или `cron.log`).

## 3. Команды

Коды выхода: 0 ok, 1 не найдено / проверка не прошла, 2 неверные аргументы или превышен бюджет, 3 нет проекта или сессии.

### Настройка

- `longrun init [--external]` - сделать cwd проектом. `--external` кладет каталог под `~/.claude/longrun/projects/` и регистрирует cwd в `registry.json`. (Флаги `--hq` и `--no-hq`, которые раньше включали слой ledger и inbox, с 0.6.0 принимаются и ничего не делают.)
- `longrun link <root | root/.longrun>` - сессии, стартующие в cwd, относятся к этому проекту. Пишет только в `registry.json`. Каталог проекта должен уже существовать. Хранилище версии 0.2 в этом cwd, если было, сворачивается: его заметки уходят в `archive/notes.md` проекта, файл переименовывается в `NOTES.legacy.md`.
- `longrun unlink` - убрать запись из реестра, ничего не удаляя.
- `longrun where` - проект, режим, сессия и ее каталог.

### Заметки

- `longrun add [--own] -t dead|decision|fact|ctx|pin|doc [-s <автор>] "<текст>"` - одна строка до 400 символов, с 0.6.0 **по умолчанию в общие заметки проекта**: факт, который стоит записать, обычно стоит записать и для того, кто придет следом, а `--shared` на каждом вызове был шагом, который модель пропускала. `--own` оставляет запись в заметках этой сессии, для того, что относится только к этому разговору (нужна известная сессия: `LONGRUN_SESSION` из окружения сессии либо самый свежий heartbeat с тем же cwd). `--shared` по-прежнему принимается и ничего не делает. Дубликат по тексту не добавляется. При переполнении - код 2 и список кандидатов на удаление.
- `longrun rm s3 n12 ...` - удалить; строки уходят в архив. Число без префикса считается своей заметкой.
- `longrun replace s3|n12 "<текст>"` - переписать; старый текст в архив.
- `longrun notes [--shared | --own | --session <кто>]` - показать; по умолчанию оба файла.
- `longrun prune [--auto] [--shared]` - кандидаты на удаление (сначала записи с пометкой stale, затем `ctx`, `fact`, `dead`/`decision`, затем `doc`; `pin` не трогается); `--auto` архивирует до 60% бюджета.
- `longrun doc add <путь> "<что внутри>"` - длинный материал остается в файле, а общие заметки несут одну строку с указателем на него: `- [n12] 09-16 doc (session): research/X.md - что внутри`. Путь хранится относительно корня проекта, поэтому одинаково разрешается из любого worktree; файл вне проекта, несуществующий файл и второй указатель на тот же файл получают отказ. `doc ls` перечисляет их с размером и mtime, `doc touch n12 "..."` переписывает описание, сохраняя путь, `doc rm n12` убирает указатель (никогда не файл).
- `longrun stale n12 "<почему перестало быть правдой>"` - это не удаление, а знак сомнения: запись продолжает показываться, но с `(STALE since ... : why)`, так что сомнение видит каждая сессия, а `prune`/`gc` берут помеченные записи первыми, каким бы ни был их тег (помеченный `pin` в том числе). `stale --clear n12` снимает пометку, `stale ls` их перечисляет. Спрашивать об этом никому не надо помнить: раз в пол-окна старения ход сессии открывается числом общих заметок, в которые с тех пор никто не заглядывал, и командой, которой одну из них помечают. Пометки лежат в `<проект>/stale.json`, рядом с `NOTES.md`, а не внутри него, потому что в этом формате заметки записаны все записи всех уже существующих проектов. Пометка не переживает свою запись.
- `longrun mute n12 [n13 ...]` / `longrun unmute [n12|--all]` - перестать подставлять общую запись в дайджест ЭТОЙ сессии. На диске ничего не меняется, для других сессий тоже; в шапке блока написано, сколько записей скрыто. Это не та половина памяти, которая подтягивает нужное обратно (она - `doc`, дельта доков и `recall`), а ее изнанка для одной сессии: сессия решает, что запись не стоит носить с собой, не решая этого за остальных.
- `longrun compact-hint [--set "<текст>" | --clear]` - что хук `PreCompact` скажет суммаризатору сохранить (раздел 4), напечатанное ровно в том виде, в каком уйдет; `--set` добавляет одну свою строку от этой сессии, `--clear` ее убирает. То же самое на весь проект - `longrun config set compact_instructions "..."`.
- `longrun recall <слово> [...] [-n 12] [--no-transcript]` - поиск. Порядок: общие и свои заметки, файлы, на которые указывают записи `doc`, чужие свои заметки и журналы, inbox, архивы; затем шесть транскриптов проекта (свежие первыми, плюс транскрипты сабагентов текущей сессии). Транскрипт - внутренний формат Claude Code, парсер защитный: при смене формата совпадений станет меньше, падения не будет.

### Сессии

- `longrun status [--all]` - сессии проекта: ключ, имя, состояние (`alive`, `alive+sock`, `stale Nm`, `active?`, `ended(reason)`), scope, число своих заметок, счетчики, последний ответ. Сессии без ходов, вызовов, заметок и ответов скрыты как шум приложения. С `--all` возвращаются и те сессии, которые пользователь заархивировал или удалил в приложении, с указанием, что именно из двух случилось.
- `longrun send [--list] [--inbox] [--resume [--max-turns N] [--model M] [--mode <permission mode>] [--timeout S]] <кто> "<текст>"|-` - адресат: заголовок из сайдбара, имя из реестра Claude Code, id или его префикс, `local_<id>`. Работает - конверт в его сокет; не работает - файл в inbox проекта адресата (не отправителя); `--inbox` - всегда файлом; `--resume` - `claude -p "<текст>" --resume <id>` в cwd адресата, ответ печатается. Неоднозначное имя - отказ со списком.
- `longrun important [on | next | N | off] [--to <кто>]` - пометить сессию, конец хода которой нельзя пропустить: пока флаг стоит, уведомление уходит независимо от `notify_turn_end` и от того, какое окно в фокусе. `on` держится до `off`, `next` взводит один конец хода, `N` - счетчик, который тратится по одному за ход и сам снимается на нуле. Без аргумента печатает флаг этой сессии и все остальные помеченные сессии проекта; `--to` принимает те же имена, что и `send`. Флаг лежит в `meta.json` сессии (`important: {mode, left}`), поэтому переживает компакцию, `/clear` и resume, и виден флагом в `status` и в шапке дайджеста.
- `longrun ask "<текст>" [--options "A,B"] [--default A] [--text] [--wait SEC] [--expire MIN]` - диалог поверх всех окон; `ask ls`, `ask answer Q3 "..."`.

### watch

`longrun watch add [--to <кто>] [--every 5m] [--for 7d] [--wake [--wake-mode M] [--wake-turns N]] [--no-notify] [--no-test] --then "<что сделать>" -- <проверка>`

| Проверка | Условие выполнено |
|---|---|
| `pr-merged <id|ветка>` | PR влит. GitHub: `gh pr view --json state` в репозитории, откуда зарегистрирован watch; Arcadia: `arc pr status --json` из `arc_root`. Инструмент выбирается при регистрации (ключ `pr_tool`: `auto` смотрит на папку, `.arc` -> arc, `.git` -> gh) и записывается в проверку. `closed`/`discarded` - жесткая ошибка |
| `pr-status <id|ветка> <open|merged|closed>` | статус равен заданному (`discarded` = `closed`) |
| `at 'YYYY-MM-DD HH:MM' | HH:MM | +2h` | момент наступил (HH:MM в прошлом = завтра) |
| `file /путь` | файл существует |
| `http URL [--expect ТЕКСТ] [--token-file ~/.tokens/x] [--auth-scheme OAuth]` | ответ 2xx и, если задано, содержит текст; 401/403/404 - жесткая ошибка, остальное - "еще нет" |
| `cmd '<shell>'` | код 0 = выполнено, 1 = еще нет, 3 = сдаться. Абсолютные пути, без alias, без Touch ID и ssh: запускается таймером с PATH, снятым с шелла при `add` |

Под таймером macOS интерпретатор может не иметь доступа к `~/Documents` (TCC): чтение конфига такого проекта тогда дает пустой конфиг, а файл сообщения пишется во второй inbox `~/.claude/longrun/sessions/<проект>/<сессия>/inbox/`, который хуки читают наравне с inbox проекта (`delivered.how` = `session-inbox:`). Ошибка доставки любого другого рода записывается в запись watch (`delivered.how` = `error: ...`) и в уведомление на рабочем столе, состояние сохраняется, тик продолжает работу: watch не повисает в pending и не стреляет повторно.

Поведение: проверка запускается сразу (`--no-test` отключает): уже верно - ничего не регистрируется; жесткая ошибка - отказ. Далее тик раз в `--every` (не чаще самого таймера, 5 минут). Три жесткие ошибки подряд - состояние `broken`, срок `--for` вышел - `expired`; в обоих случаях адресат получает одно сообщение. Доставка: сокет, если адресат работает; `claude -p --resume` при `--wake`; иначе файл в inbox. Плюс уведомление на рабочем столе. Запись хранит стабильный id приложения адресата, поэтому переименование и resume ничему не мешают. Другие подкоманды: `ls [--all]`, `rm <id>`, `test -- <проверка>`, `sessions`, `run [--force] [id]`, `install [--every 5m]`, `uninstall`, `status`.

### Служебные

`longrun digest [--source startup|compact|...]` (что печатает хук), `longrun gc` (уборка сейчас), `longrun hook <Событие>` (вызывается хуками, JSON на stdin), `longrun help`, `longrun rules`, `longrun version`.

`longrun onboard` печатает бриф для первого разговора о скилле: что делает, нюансы, состояние машины (проект, таймер, окно автокомпакции в Claude Code против ключа `autocompact_window`) и список настроек, о которых стоит спросить пользователя, с текущими значениями и смыслом. Агент пересказывает его на языке пользователя, спрашивает по одной и применяет ответы через `config set`; `longrun onboard done` записывает `onboarded_at` в глобальный конфиг, после чего дайджест перестает подсказывать `longrun onboard`.

`longrun config` показывает действующие настройки и откуда каждая взята (default / global / project); `longrun config set KEY VALUE` пишет в `config.json` проекта, с `--global` в `~/.claude/longrun/config.json`; `longrun config unset KEY [--global]` возвращает значение по умолчанию. Тип значения проверяется по умолчанию ключа (число, дробь, true/false), незнакомый ключ отклоняется.

## 4. Хуки

Двенадцать записей в `~/.claude/settings.json`, все вызывают `longrun hook <Событие>`. Проект резолвится по `_index` для всех событий, кроме `SessionStart(startup|resume|fork)`, где он резолвится по cwd и записывается в `_index`. Вне проекта хук молча выходит с кодом 0.

| Событие | Что делает | Что печатает в контекст |
|---|---|---|
| `SessionStart` (все источники) | привязка CLI id к сессии (цепочка resume, `/clear`, fork), heartbeat, строка в журнал, экспорт `LONGRUN_SESSION`, `LONGRUN_DIR`, `LONGRUN_SCOPE`, `LONGRUN_TRANSCRIPT` через `CLAUDE_ENV_FILE`, раз в час gc, отметка "все общие записи показаны" | дайджест (раздел 5) |
| `UserPromptSubmit` | счетчик ходов; сброс того, что меряется за ход (список правленых файлов под дельтой доков, лимиты подсказок); дочитывание метаданных приложения и перепривязка цепочки | сообщения из inbox; свои заметки один раз при перепривязке; дельта общих заметок; дельта сессий (одна появилась, закончилась или сказала что-то новое); дельта доков; карточка хода: что изменил только что закончившийся ход и что в нем упало, не чаще раза в 8 ходов и только если ход что-то правил или ронял, но ничего не записал; вопрос про уборку (не перестало ли что-то из старых общих заметок быть правдой) - у него свои условия и ни одного условия карточки: не в первые 4 хода сессии, не чаще раза в пол-окна старения и только когда есть что перечитывать |
| `PostToolUse` (Edit, Write, MultiEdit, NotebookEdit, Bash, PowerShell, Agent, Task, Workflow, Grep, Glob, WebSearch, mcp__*) | счетчики; правками считаются только четыре редактирующих тула; список правленых файлов. Три поисковых тула не двигают ни одного счетчика - они подключены ради запроса внутри них и больше ни для чего | сообщения из inbox как `additionalContext`; дельты общих заметок и доков, чтобы заметка соседней сессии не ждала, пока человек нажмет Enter (не чаще раза в 5 вызовов и только чужие изменения); подсказка про уже записанное (раздел 5a); подсказка про поллинг (раздел 5b); та же карточка для идущего хода, когда с последней заметки или карточки прошло окно в 40 вызовов |
| `PostToolBatch` (без матчера) | одно событие на всю пачку параллельных вызовов, после того как все они разрешились. Запоминает файлы, которые пачка открывала, включая Read - на них матчер `PostToolUse` намеренно не тратит процесс. Событие новее остальных: где CLI его не знает, запись просто инертна, и подсказку несет один `PostToolUse` | подсказка про уже записанное на всю пачку, как `additionalContext` |
| `PostToolUseFailure` (Bash, PowerShell; async) | строка `FAIL` в журнал, хранятся последние 40 | - |
| `PreToolUse` (все инструменты) | при `halt` отказ `permissionDecision: deny` с причиной (кроме вызовов самого `longrun`); для Bash, PowerShell, Agent, Task, Workflow и MCP-тулов запись в `running` меты (команда, начало) | отказ с причиной при halt, иначе ничего |
| `PermissionRequest` (async) | `waiting_since` / `waiting_what = permission (<tool>)` в мете | - |
| `Notification` (permission_prompt, idle_prompt, agent_needs_input, elicitation_*; async) | ожидание подтверждения или ввода в мете; `idle_prompt` снимает `turn_started` | - |
| `PreCompact` | снимок в `archive/precompact/` | инструкции суммаризатору (ниже) |
| `PostCompact` (async) | резюме в `archive/compact/` | - |
| `Stop` | последний ответ в `last_status`; ход замораживается в `turn_card` для следующего промпта | - |
| `SessionEnd` | причина в heartbeat, строка в журнал | - |

Порядок событий вокруг компакции: `SubagentStop` (суммаризатор) -> `SessionStart(compact)` -> `PostCompact`. Поэтому дайджест после компакции называет каталог архива, а не файл: текущее резюме ложится туда через мгновение после дайджеста.

### Как управлять самой компакцией

Автоматическая компакция и `/compact <текст>` собирают **один и тот же** промпт суммаризатора (тот самый, из восьми секций: запрос и намерение пользователя, технические понятия, файлы и код, ошибки и починки, решение задач, все сообщения пользователя, незакрытые задачи, текущая работа). Разница ровно одна: непустой текст инструкций приклеивается к нему в конец блоком `Additional Instructions:`. То есть автокомпакция - не другой, более глупый механизм, а тот же самый, только без подсказки, которую человек пишет руками.

Этим блоком становится stdout хука `PreCompact`. Проверено чтением Claude Code 2.1.238: результаты хуков с кодом возврата 0 и непустым stdout склеиваются через перевод строки в `newCustomInstructions`, мерджатся с тем, что пользователь написал после `/compact`, и уходят в сборку промпта; матчер срабатывает и на `trigger: "auto"`, не только на `"manual"`. Отсюда два следствия: текст не стоит контекста (его пишет скрипт, а не модель), и человеку больше не надо ловить момент перед компакцией, чтобы ею управлять.

`longrun` печатает его из `compact_hint()`: четыре постоянные строки (сохранить тупики вместе с причиной; сохранять точные строки, а не их описания; выкинуть то, что вернет один Read; сохранить формулировки самого пользователя), затем выведенное из состояния сессии - задачи этой сессии на доске, файлы, которые она правила, id заметок, которые и так переинжектятся сразу после компакции, - затем строка проекта `compact_instructions` и строка сессии из `longrun compact-hint --set`. При переполнении `compact_hint_max_bytes` первыми уходят выведенные строки (файлы, заметки, задачи, вводная), постоянное ядро и две человеческие строки остаются. `longrun compact-hint` печатает ровно то, что отправит хук; `compact_hint false` выключает все это.

Грабли: stdout, который разбирается как JSON, читается вместо этого как объект решения хука (а `decision: "block"` отменяет компакцию - сессия продолжается несжатой), поэтому текст простой; тот же предел примерно в 10000 символов на вывод хука действует и здесь; этот же текст показывается в транскрипте строкой результата хука.

## 5. Дайджест и бюджеты

Состав по порядку: заголовок `<longrun v… project=… session=<имя> [<ключ>] source=…>`; MESSAGE (недоставленные сообщения); `SHARED notes (project X) N entries used/cap`; `OWN notes (this session)`; `SESSIONS of project X` (до 6 других сессий: имя, ключ, состояние, scope, число своих заметок, два последних каталога, в которых сессия правила файлы, последний ответ; живые первыми - чтобы "передать более релевантной сессии" работало, релевантность должна быть видна, а заголовок окна - это то, как его назвал человек); `WATCH n pending`; при оркестраторе еще `ORCHESTRATOR`, `BOARD`, `YOUR TASK`, `FACTS unhandled`, блок обязанностей и `ASKED` (раздел 11); `HANDOFF` (только после компакции: FAIL-строки, `FOCUS:` из `/compact <текст>`, `ASK:` последние просьбы, `FILES:`); хвост журнала (после компакции, resume, `/clear`, fork и при продолженной цепочке); строка про архив резюме; подсказка команд.

Бюджеты по умолчанию: общие заметки 5000 байт, свои 3000, дайджест 9000 (в контекст встроенным текстом приезжает примерно 10000 символов вывода хука: все сверх этого Claude Code 2.1.238 пишет в файл и оставляет ссылку, а сборки постарше просто обрезали), блок сессий 700, HANDOFF 1100, дельта общих заметок на ходу 1200. При превышении дайджест сбрасывает блоки в порядке: watch, журнал, обязанности оркестратора, сессии, ASKED, факты, доска, HANDOFF.

И только потом уступают заметки, а уступают они так: **по одной сбрасывают самые старые записи, кроме `pin` и `doc`** (строка `doc` - это указатель на материал, который нарочно держат вне контекста, так что выбросить ее значит спрятать целый файл, а не один факт), от старых к новым сразу по обоим файлам, и добавляют строку о том, сколько записей не поместилось. Обрезка готового хвоста, как было до 0.6.0, убирала ровно те записи, которые записали последними. Если не хватает и этого, текст обрезается с закрывающим тегом.

`notes_max_bytes` - жесткий потолок *файла*, за который `add` не пускает. До сессии же доезжает то, что ограничено `inject_max_bytes`, поэтому все, что судит "файл становится слишком большим" (строка `PRUNE NEEDED`, автопрун в `gc`), меряет от `inject_max_bytes` минус 600 байт шапки и подсказок, деля их между двумя файлами пропорционально их потолкам, когда те просят больше. `config set` отказывает в комбинации, где `notes_max_bytes + session_notes_max_bytes + 600` выше `inject_max_bytes`: эти байты не пожелание на будущее, а записи, которых не увидит ни одна сессия. Проект, настроенный до появления этой проверки или поправленный руками, все еще может быть в таком состоянии и снаружи выглядит здоровым - поэтому в шапке блока заметок пишется `(only ~N B of these fit a digest: longrun config)`, как только потолок больше того, что влезает в дайджест, а `longrun config` печатает всю арифметику предупреждением под настройками.

Блок SESSIONS - снимок на SessionStart, поэтому дельта на ходу несет то, что изменилось с тех пор: сессия появилась, сессия закончилась или одна из них дала новый последний ответ (`sessions_delta_max_bytes`, намеренно грубая: работающий инструмент или процент контекста меняются каждую минуту и строки не стоят). Сессии, которые пользователь заархивировал или удалил в приложении, не попадают ни в блок, ни в дельту и не попадают в `longrun status` без `--all`, который заодно говорит, что именно из двух случилось; каталог удаленной `gc` архивирует сразу, не дожидаясь `session_dead_days`, а каталог заархивированной - никогда, потому что пользователь может вернуть ее из архива.

Дельта на ходу так же слушается `shared_delta_max_bytes`: показывает измененные записи от старых к новым, помечает увиденным **только то, что напечатала**, и пишет `(+N more on your next turn)`. Не поместившееся приходит следующим ходом, а не пропускается молча: раньше оно помечалось увиденным и тут же срезалось, то есть сессия его не видела вовсе.

**Слой доков.** Запись `doc` - ленивая половина памяти: длинный материал лежит в файле внутри проекта, общие заметки несут одну строку с его путем и с тем, что внутри, а сессия читает файл, только когда он ей нужен. Указатель держат честным три вещи, и все они берутся из одного `stat` на запись: файл, записанный после своего описания, рисуется как `(file changed MM-DD HH:MM, this line may be stale)`, пропавший файл рисуется как `(MISSING: no such file now)`, и через семь дней `gc` архивирует такую запись, а файл, изменившийся с тех пор, как сессия в него заглядывала, приходит ей на следующем ходу строкой `DOCS changed`.

Точка отсчета для "после своего описания" - mtime файла в момент, когда отработал `doc add` или `doc touch`; он хранится по записям в `<проект>/docs.json`. Раньше отсчет шел от даты самой заметки, а в заметке нет времени суток - поэтому правка, сделанная через несколько часов в **тот же день**, читалась как отсутствие правки. Это и есть главный случай: другое окно переписало материал, пока эта сессия была закрыта. У проекта, заведенного до появления `docs.json`, отметок нет, и он откатывается на старое сравнение с точностью до суток, а не замолкает. `gc` убирает отметки, чей указатель исчез.

`DOCS changed` читается в две стороны: чужая правка - это предложение прочитать файл, если он нужен задаче (`~`), а файл, который правила **эта** сессия, - долг: описание, которое читают все остальные, описывает файл до правки, поэтому строка просит `doc touch n<id>` (`!`). Авторство берется из правящих тулов самой сессии **за только что закончившийся ход**; раньше это был список на всю сессию, так что стоило сессии один раз тронуть файл, и любая чужая правка в нем до конца сессии подписывалась "ты". Внутри хода отдается только половина `~`: чужая правка - новость, которая нужна сразу, а долг иначе срабатывал бы после каждого сохранения того же файла. `recall` ищет и внутри файлов, на которые указывают записи, ранжируя их сразу после самих заметок. Один только возраст `doc` никогда не архивирует: названный файл может быть многомесячной давности и при этом ровно тем, что нужно.

Стоимость: дайджест 350-2000 токенов в зависимости от заполненности; на ходу - только разница и сообщения, обычно ноль.

## 5a. Подсказка про уже записанное

Все остальное предлагает сохраненный материал **заранее**: дайджест на каждом старте, указатели на доки в нем, дельта доков, когда файл поехал. Обратное направление - подтянуть записанное в тот момент, когда оно понадобилось, то есть пункт 2 манифеста, - держалось на том, что модель вспомнит про `longrun recall`, а это инструкция, а не механизм. Эта подсказка и есть механизм.

Сигнал - сессия, называющая свой вопрос вслух: `pattern` у `Grep` или `Glob`, запрос у `WebSearch`, однострочное `description` у субагента. У `Read` вопроса нет, и путь - тоже не вопрос: угадывать намерение по пути и есть способ превратить такую подсказку в шум, да и сессия к этому моменту уже смотрит в нужное место.

Ищет она в **курируемой** части того, где ищет `recall`, минус то, что уже в контексте: файлы за указателями `doc`, заметки других сессий и заметки, которые архив сохранил при чистке. Общие заметки и свои заметки сессии целиком инжектятся на каждом старте, поэтому попадание в них - это перечитанный дайджест, а не новость. Файлы, которые сессия уже открывала, пропускаются по той же причине; на CLI с `PostToolBatch` сюда попадает и то, что она просто прочитала.

Журналы, снимки перед сжатием и сохраненные саммари **не** ищутся, и эта граница не придумана, а измерена. `recall` их ищет, и правильно делает: человек спросил сам и может оценить, что нашлось. Неспрошенная подсказка так не может. Прогон настоящей истории поисков этого проекта (`research/measure-recall.py`) через первую версию показал: срабатывает на половине поисков, и две трети попаданий - строки журнала вроде "PR #8 opened" или архивная `ctx`-заметка про ветку. Слова совпали, вопрос не отвечен.

Остальное отсекают два правила, и оба взялись из того же прогона:

- **В файле заметок считаются только теги, которые причитаются другой сессии**: `dead`, `decision`, `fact`, `pin`, `doc`. `ctx` и `todo` - собственная бухгалтерия одного разговора; одна архивная `ctx`-заметка давала треть всего шума в первой версии.
- **В doc-файле одного слова мало.** Проза совпадает с обычным словом случайно, два разных слова на одной строке - гораздо реже. Поэтому однословный `Grep` в прозу не указывает никогда, а описание субагента или запрос из нескольких слов - может.

Объем держат еще четыре вещи: слово не короче 5 символов; слово, найденное больше чем на 6 строках, - это слово, а не термин; одна и та же пара (слово, файл) не показывается дважды; за ход срабатывает не больше двух подсказок, и лимит обновляется на границе хода. Один проход по материалу ограничен 600 КБ, сколько бы ни лежало в проекте. Подсказка цитирует максимум две строки и передает дальше в `longrun recall <слово>`. На прогоне это выходит около 1.5 подсказки на сессию.

## 5b. Подсказка про поллинг

`watch` существует потому, что сессия, ждущая руками, тратит ход модели на каждую проверку и перестает ждать вместе с разговором. Выбор в его пользу был советом в `SKILL.md` и больше ничем. Теперь вызов `Bash` или `PowerShell`, который спит 30 секунд и дольше, или крутит цикл вокруг sleep, или третий раз за ход повторяет ту же команду, один раз за ход получает в ответ то, что сделал бы `watch`. Это подсказка, а не отказ: долгий sleep иногда именно то, что нужно, и отличить одно от другого тут нечем.

## 6. Конфигурация

Восемь ключей вышли из обращения и стали в скрипте постоянными числами: `strict_stop` и `auto_init` (оба выключены, и оба - режим, которым никто не пользовался), `journal_tail_lines` (12), `handoff_max_bytes` (1100), `fail_keep` (40), `recall_transcripts` (6), `ctx_sample_every` (5) в 0.6.0 и `compact_hint_max_bytes` (1200) в 0.6.1. Настройка, которую ни разу не меняли, - не настройка. То, что добавила 0.6.2, зафиксировано по той же причине: вопрос про уборку ждет 4 хода, дельта внутри хода срабатывает не чаще раза в 5 вызовов, подсказка про уже записанное дает 2 срабатывания за ход по словам от 5 символов, найденным не более чем на 6 строках, а подсказка про поллинг ловит sleep от 30 секунд или третью одинаковую команду. Вышедший из обращения ключ, оставшийся в config.json, не читается, показывается в `longrun config` как retired и убирается через `config unset`; на `config set` такой ключ отвечает тем, что его заменило, а не словами "unknown key".

`~/.claude/longrun/config.json` (глобально) и `<проект>/config.json`; значение проекта перекрывает глобальное. Смотреть и менять: `longrun config`, `longrun config set KEY VALUE [--global]`, `longrun config unset KEY [--global]`. Ключи и значения по умолчанию:

| Ключ | Значение | Смысл |
|---|---|---|
| `notes_max_bytes` | 5000 | бюджет общих заметок |
| `session_notes_max_bytes` | 3000 | бюджет своих заметок |
| `notes_warn_pct` | 80 | выше - PRUNE NEEDED в дайджесте |
| `notes_autoprune_pct` | 90 | выше - gc архивирует сначала записи с пометкой stale, затем `ctx`, до `notes_warn_pct`; 0 отключает |
| `notes_max_age_days` | 14 | записи старше уходят в архив (кроме `pin`) |
| `journal_max_lines` | 200 | длина журнала, после которой хвост уезжает в архив |
| `inject_max_bytes`, `sessions_block_bytes`, `shared_delta_max_bytes`, `sessions_delta_max_bytes` | 9000, 700, 1200, 500 | бюджеты дайджеста; последние два - порции на один ход |
| `archive_keep_days`, `compact_keep` | 30, 40 | хранение архива; максимум резюме на проект |
| `compact_hint` | true | шлет ли `PreCompact` суммаризатору инструкции (раздел 4); их потолок - постоянные 1200 B |
| `compact_instructions` | пусто | еще одна строка к ним, общая для всех сессий проекта ("каждый SQL-запрос сохранять целиком") |
| `session_stale_min`, `session_dead_days` | 30, 7 | когда сессия `stale`; когда ее каталог уезжает в архив |
| `nudge_turns`, `nudge_tools`, `nudge_edit_tools` | 8, 40, 6 | карточка хода: как часто максимум (ходы), окно внутри хода (вызовы инструментов) и сколько правок нужно, чтобы про ход стоило спросить. Оба окна отсчитываются от того, где счетчики стояли на прошлой карточке, и никогда не берутся по модулю: счетчик, перешагнувший границу (`PostToolUseFailure` начисляет единицу, не глядя на карточку), раньше пропускал целое окно |
| `name` | не задано | имя проекта вместо имени папки |
| `pr_tool` | `auto` | чем спрашивать статус PR в проверках `pr-merged`/`pr-status`: `gh` (GitHub CLI), `arc` (Arcadia), `auto` - по папке, из которой зарегистрирован watch |
| `arc_root` (только глобально) | `~/arcadia` | откуда звать `arc pr status`, только для Arcadia |
| `watch_timer` (только глобально) | `auto` | чем гоняется тик watch: `launchd` (macOS), `systemd` (пользовательский таймер), `cron`, `none` (не гоняется вовсе, `longrun watch run` планируете сами). `auto` - launchd на macOS, systemd-таймер на Linux, cron там, где пользовательского systemd нет |
| `notifier` (только глобально) | `auto` | чем рисуется уведомление: `terminal-notifier`, `osascript`, `notify-send`, `none`. `auto` - terminal-notifier, если он поставлен (на macOS osascript доставляется, но не рисуется, раздел 12), иначе то, что дает ОС. Что на этой машине выходит на самом деле, скажет `longrun notify --test` |
| `notify_turn_end` (только глобально) | `off` | уведомление, когда сессия закончила ход: `off`, `unfocused` (только пока приложение Claude не в фокусе), `always`. Само приложение это событие шлет, но тихим и с `interruptionLevel: passive`, поэтому macOS кладет его в Центр уведомлений, не рисуя баннера - тут громкий и кликабельный вариант того же самого |
| `autocompact_window` | 0 | где Claude Code делает автокомпакцию, как задано `/autocompact N`; принимает `300k`, `1M`. 0 - следовать за Claude Code (переменная `CLAUDE_CODE_AUTO_COMPACT_WINDOW`, затем `autoCompactWindow` в settings.json, затем окно модели). Ключ - намерение пользователя: хук не может выполнить `/autocompact`, поэтому `onboard` сравнивает его с settings.json и просит пользователя ввести команду самому |
| `board_block_bytes`, `facts_block_bytes` | 900, 900 | место в дайджесте под доску и под необработанные факты |
| `ctx_warn_before` | 50000 | за сколько токенов до окна автокомпакции предупредить сессию (сам размер читается из транскрипта каждый пятый вызов инструмента, и это больше не настройка) |
| `stuck_tool_min`, `stuck_wait_min` | 30, 10 | два порога смотрителя: инструмент все еще работает, неотвеченный запрос прав или диалог (минуты) |
| `stuck_fail_streak` | 3 | сколько одинаковых FAIL подряд, прежде чем в строке SESSIONS появится `fail x3` (это флаг, а не отчет) |
| `wake_on_stuck` | true | отчет о затыке начинает ход у оркестратора (иначе ложится в inbox) |
| `ask_wait_sec`, `ask_expire_min`, `ask_beep` | 90, 360, true | сколько секунд `ask` ждет ответ инлайн; через сколько минут диалог закрывается сам (0 = никогда); звук перед показом |
| `note_max_chars` | 400 | длиннее `add` отказывает: один факт, одна строка |
| `watch_ttl_days`, `watch_check_timeout_sec`, `watch_hard_errors` (только глобально: тик идет без проекта) | 7, 90, 3 | срок watch по умолчанию (`--for` перекрывает); таймаут одной проверки; сколько жестких ошибок подряд до "broken" |

## 7. Сообщения между сессиями (механика)

Реестр сессий Claude Code: `~/.claude/sessions/<pid>.json` (`sessionId`, `name`, `cwd`, `messagingSocketPath`). Живая сессия принимает на unix-сокете одну JSON-строку `{"msgV":1,"msg_id":…,"type":"user","message":{"role":"user","content":…},"priority":"next","from":"uds:…"}`; без `priority: "next"` простаивающая сессия ход не начинает. Тело обернуто в `<cross-session-message from-name=… from-mode=…>`, как делает собственный `SendMessage` Claude Code 2.1.260; получатель видит `Message from @<имя>` и сохраняет свои права. Формат взят из бинаря, не из доков.

Файл в inbox доставляют хуки адресата: `SessionStart` (в дайджесте), `UserPromptSubmit`, `PostToolUse` (`additionalContext`). Файл адресуется стабильным id приложения, когда он известен, поэтому переживает resume; хуки читают оба адреса. Сообщение кладется в проект адресата по `_index`, даже если отправитель в другом проекте.

`--resume`: `claude -p "<текст>" --resume <id> --max-turns N` в cwd адресата, из окружения убраны `LONGRUN_*` и `CLAUDE_ENV_FILE`, чтобы поднятая сессия привязалась к своему проекту. Без диалогов прав: годится "проверь и доложи", не "выкати".

## 8. Миграция с 0.2

Автоматически, при первом обращении:

- хранилище worktree (`~/.claude/longrun/projects/<slug>/` с `config.hub`) - проектом становится хаб; заметки старого хранилища уходят в `archive/notes.md` проекта с пометкой `legacy`, файл переименовывается в `NOTES.legacy.md`; запись реестра при следующем `longrun link` начинает указывать прямо на проект;
- плоские файлы сессий `<sid8>.json` и `<sid8>.md` раскладываются в `<sid8>/meta.json` и `<sid8>/journal.md`; каталог `sessions/` внутри проекта версии 0.1 переезжает туда же;
- заметки хаба остаются общими заметками проекта как есть (id `n…` не меняются); своих заметок у старых сессий нет, они появляются с первого `longrun add`.

Сессии, идущие в момент обновления, ничего не теряют: каждый хук сначала переносит файлы старой раскладки и только потом читает их, а если сессия уже успела записать что-то в новую раскладку, старое и новое сливаются (счетчики суммируются, строки журнала идут по порядку).

Что меняется в привычках: `rm`/`replace` принимают `s3` и `n12`, бюджет общих заметок 5000 вместо 6000, а у сессии появились свои заметки - `longrun add --own`. (В 0.3.0-0.5.1 `add` без `--shared` писал именно в них; с 0.6.0 все наоборот, см. раздел 3.)

## 9. Тесты

`bash tests/run.sh` - 222 проверок регрессии на синтетических данных без вызовов API: настройка и привязка worktree, общие и свои заметки (дедупликация, бюджет, отказ на границе, prune, rm, replace), слой доков и пометки stale, все хуки на реальных формах payload, снимок и архив компакции, инструкции, которые хук PreCompact отдает суммаризатору, HANDOFF, привязка хуков к сессии при уходе шелла в другой каталог, карточка хода и ее каденция, вопрос про уборку сам по себе в ходу без единой правки, дайджест при всех полных бюджетах (и что указатель `doc` никогда не уступает первым), ранжирование recall, 20 параллельных писателей в оба файла, gc и старение, миграция трех старых раскладок, notes-only режим, send в сокет и в inbox с доставкой всеми тремя хуками, resume по заголовку сайдбара, watch с тиком без таймера, команда config (set, unset, --global, проверка типа, лимиты из настроек, предупреждение о бюджете, выставленном до появления проверки, вышедший из обращения ключ), onboard (бриф, подсказка в дайджесте, done), выбор gh/arc для проверок PR и слой платформы (какой планировщик достается этой ОС, расписание cron и правка crontab, юниты systemd, какая программа рисует диалог, чем рисуется уведомление и умеет ли машина сказать, что его правда показали, цель клика по уведомлению, правило конца хода и как читается активное приложение, флаг важной сессии через хук Stop: взвод, обратный отсчет, самоснятие и то, что он обходит обе проверки).

`bash tests/scenarios.sh` - 37 проверок в девяти сценариях, по одному на задачу из README: ориентация новой сессии, свои заметки через компакцию, дельта общих заметок на ходу, две сессии в одной папке, resume под новым CLI id (сразу и с опозданием метаданных приложения), `/clear`, fork, передача работы (inbox, сокет, watch по имени) и живая картина остальных сессий (одна появилась, одна закончилась, одну заархивировали в приложении). Выводы последних прогонов: [tests/last-run.txt](../tests/last-run.txt), [tests/last-run-scenarios.txt](../tests/last-run-scenarios.txt).

`bash tests/orchestrator.sh` - 51 проверка слоя оркестратора (замок и зеркало, доска, факты, halt через PreToolUse, телеметрия, два правила смотрителя про затыки). `bash tests/ask.sh` - 27 проверок диалога и MCP-сервера на фикстуре (ответ инлайн и ходом, отказ/истечение/сбой, исключение из halt, флаг у смотрителя, JSON-RPC по stdio). Во всех наборах `LONGRUN_NO_UI=1`: настоящих диалогов и уведомлений тесты не показывают.

Живой прогон на `claude -p` (стоит денег): [tests/e2e-claude.md](../tests/e2e-claude.md).

## 10. Что гарантировано, что вероятностно, что сломается при смене версии

**Механикой:** заметки и журнал возвращаются в контекст на каждом `SessionStart`, включая компакцию, `/clear`, resume и fork (проверено на транскрипте); каждое резюме компакции архивируется целиком; упавшие команды попадают в журнал; бюджеты нельзя превысить через CLI; параллельные записи не теряются (`flock` + атомарная замена); сообщения доставляются один раз; дельта общих заметок считается по хешам текста, а не по времени.

**Только вероятностно:** что модель напишет заметку, отметит `--shared` то, что нужно другим, вызовет `recall` вместо повторного исследования, будет удалять. Anthropic закрыл как not planned просьбу дать модели гарантированный ход перед компакцией (issue anthropics/claude-code#43733). Разделение с автопамятью держится на инструкции.

**Сломается при смене версии Claude Code:** формат транскрипта (recall, снимок перед компакцией: меньше совпадений, снимок короче); формат сокета и реестра сессий (`send` в живую сессию; очередь через inbox продолжит работать); формат файлов приложения (цепочка resume, разрешение имен из сайдбара; без них сессия после resume в приложении начнет с пустыми своими заметками, старые останутся на диске и в `recall`); имена полей событий хуков; предел примерно в 10000 символов на встроенный вывод хука (не документирован, и поведение при переполнении уже менялось: сначала обрезка, в 2.1.238 сброс в файл); `CLAUDE_ENV_FILE` (без него сессия определяется по cwd, что ломается при двух сессиях в одной папке); подхват хуков на лету (проверен эмпирически, в доках не обещан).

**Не покрыто:** сабагенты дайджест не получают; Windows не проверялся (`fcntl`, unix-сокеты, ни один из трех планировщиков); `/rewind` заметки не откатывает; при resume в истории остаются два дайджеста до ближайшей компакции (поведение Claude Code); две сессии, сделавшие `/clear` в одной папке за три минуты, могут связаться не с тем ключом.

**Проще, если нужна только ре-инъекция:** строка `@.longrun/NOTES.md` в `CLAUDE.md` приезжает после компакции без хуков и без лимита вывода хука. Хуки нужны ради своих заметок, архива резюме, журнала падений, HANDOFF, сессий и сообщений.

## 11. Оркестрация (0.4.0, урезана в 0.6.0)

Одна сессия проекта берет роль, остальные докладывают через доску, и она же зовет человека, когда решить может только человек. Это не самостоятельное движение к цели: одна сессия к одному проверяемому условию - это собственный `/goal` Claude Code, у которого есть настоящий оценщик. Что 0.6.0 убрал из этого слоя и почему - раздел 0 в [ORCHESTRATOR.ru.md](ORCHESTRATOR.ru.md); весь остальной файл - полный дизайн.

### Файлы

| Файл | Кто пишет | Что внутри |
|---|---|---|
| `<проект>/board.json` | `board` | `goal`, `items` (задачи `T<n>`: text, state todo/doing/blocked/done/dropped, owner/owner_name, assigned, since, why, outcome, after; факты `F<n>`: text, source, task, handled, ack), счетчики `next_t`, `next_f` |
| `<проект>/orchestrator.json` и зеркало `~/.claude/longrun/projects/<key>/orchestrator.json` | `orchestrate start/stop` | skey, sid, name, pid, since, cwd, local; зеркало читает смотритель (проект под `~/Documents` ему недоступен) |
| `~/.claude/longrun/halt.json` | `halt`, `resume` | reason, at, by, local (пусто = все проекты) |
| `<сессия>/meta.json`, новые поля | хуки | `running` {tool_use_id: tool, cmd, started}, `turn_started`, `waiting_since`, `waiting_what`, `ctx_tokens`, `ctx_model`, `ctx_window`, `ctx_at`, `ctx_warned`, `ctx_told`, `board_mtime`, `board_seen`, `asked` |
| `~/.claude/longrun/watch/state.json`, ключ `stuck` | смотритель | поднятые флаги `<key проекта>:<skey>:<tool|wait|ask>` -> время; снимаются, когда условие ушло |
| `<проект>/archive/board.md` | `board rm` | удаленные задачи |
| `<сессия>/asks.json` | `ask`, отвязанный процесс `ask _wait` | вопросы `Q<n>`: q, options, default, free_text, state pending/answered/cancelled/expired/failed, answer, text, asked, answered, claimed (спросивший еще ждет инлайн), delivered (socket / inbox:<файл> / spool:<файл>) |
| `~/.claude/longrun/ask.log` | отвязанные процессы диалогов | вывод и ошибки osascript, zenity или kdialog |

### Команды

| Команда | Что делает |
|---|---|
| `board [ls [--all] [--facts]]` | цель, doing, blocked, следующие todo, счетчики; `--all` добавляет done/dropped, `--facts` показывает вместо них факты |
| `board goal "..."` | цель проекта |
| `board add "..." [--after T3,T4] [--for WHO] [--wake]` | задача; `--for` сразу выдает сессии (сообщение в inbox, `--wake` в сокет) |
| `board take T7 [--force]` | взять; отказ, если задача ждет `--after` или ее делает другая сессия |
| `board done T7 ["итог"]`, `board block T7 "почему"` | закрыть / заблокировать; будят оркестратора (сокет, если он жив, иначе inbox); `--quiet` не будит |
| `board drop|release|edit|rm T7 [...]` | снять, вернуть в todo, переписать текст, удалить |
| `board assign T7 WHO [--quiet] [--force]` | выдать сессии: сообщение `YOUR TASK` в ее сокет (ход начинается сразу) или в inbox |
| `board add --fact "..." [--task T7] [--source user\|review\|msngr\|watch\|session] [--wake]` | записать то, что узнали снаружи; попадает в inbox оркестратора (`--wake` в сокет). Факт - запись того же `board.json`, поэтому с 0.6.0 это подкоманда `board`, а не отдельная команда |
| `board ls --facts [--all]`, `board ack F3 ["решение"]` | те, которыми еще никто не занимался; пометить разобранным с указанием, что решили |
| `orchestrate [status]` | кто оркестратор, halt, доска, факты, сессии с флагами |
| `orchestrate start [--goal "..."] [--force]`, `orchestrate stop [--force]` | взять роль (одна на проект) / отдать |
| `halt "почему" [--project]`, `resume` | запрет инструментов во всех сессиях (или только в этом проекте) / снятие |
| `ask "вопрос" [--options "Да,Нет"] [--default Да] [--text] [--title T] [--wait SEC] [--expire MIN] [--icon note|caution|stop]` | диалог поверх всех окон (osascript из собственного процесса со звуком на macOS, zenity или kdialog на Linux); ответ инлайн, если пришел за `--wait` (по умолчанию `ask_wait_sec`), иначе `PENDING Q<n>` и ответ ходом в сокет или inbox; строки `ANSWER|CANCELLED|EXPIRED|FAILED Q<n>` |
| `ask ls`, `ask answer Q3 "..."` | вопросы этой сессии; записать ответ, который человек дал в чате, когда диалог пропал |
| `mcp` | stdio MCP-сервер с тулами `ask` и `notify`; `install.sh` регистрирует его как `longrun` в user scope (`claude mcp add --scope user longrun -- ~/.local/bin/longrun mcp`) |
| `notify "text" [--title T]` | одно уведомление на рабочий стол - то же самое шлют `halt` и сработавший watch; печатает, через какую программу ушло, и выходит с 1, если такой программы нет |
| `notify --test` | послать пробу и сказать, показал ли ее экран на самом деле: на macOS ответ читается из базы Центра уведомлений, а не угадывается. `presented=0` - доставлено и молча выброшено, и тогда в выводе написано, чем это чинится |

### Что видит каждая сессия

В дайджесте: строка `ORCHESTRATOR: <имя> [skey] alive|idle|stale|ended since ...`, блок `BOARD`, `YOUR TASK` для выданных задач, `HALT in force` при остановке, флаги в строках `SESSIONS`. На каждом ходу и после инструментов: `BOARD changes for you` (выданные и измененные задачи, новые факты по своим задачам; свои правки не повторяются), предупреждение `context Nk of the Mk auto-compact window` за `ctx_warn_before` до окна, один раз на 20k роста. Оркестратор дополнительно получает `role=orchestrator` в заголовке, блок `FACTS unhandled` и список обязанностей. Сессия, задавшая вопрос через `ask`, до ответа видит блок `ASKED, waiting for the user`, остальные - флаг `asking the user Q3 12m` в ее строке SESSIONS.

### Смотритель

Каждый тик после проверок `stuck_scan`: для каждого проекта с зеркалом оркестратора и живой ролью, для каждой сессии кроме оркестратора - два правила: инструмент работает дольше `stuck_tool_min` и ожидание ответа на запрос прав или на `ask` дольше `stuck_wait_min`. И то и другое значит одно: сессия не двигается, а изнутри этого никто не скажет. Еще три правила убраны в 0.6.0 - ход дольше часа (так выглядит обычный длинный ход), три одинаковых FAIL (их карточка хода и так кладет перед самой сессией, вместе с командой) и контекст у границы окна (он и так доходит до сессии предупреждением, а до оркестратора через `ctx_told`). Все три по-прежнему показываются флагами в SESSIONS. Если у сессии есть живой pid, а в дереве процессов нет обертки shell-snapshot, флаг `tool` не поднимается (мета устарела). Каждый флаг сообщается один раз на эпизод: `STUCK? <имя> [skey]: ... Options: ...`, доставка в сокет (`wake_on_stuck`) или inbox.

### Окно контекста

`autocompact_window(model)`: `CLAUDE_CODE_AUTO_COMPACT_WINDOW`, иначе `autoCompactWindow` из `~/.claude/settings.json` (его пишет `/autocompact 300k`), иначе окно модели (1M у Fable, Sonnet 5, Opus 5; 200k у остальных); не больше окна модели. Размер контекста = `input + cache_read + cache_creation` последнего ответа ассистента из хвоста транскрипта (256 КБ), та же формула, что у строки статуса.

### Диалог с человеком

`ask` показывает диалог той программой, которая есть у этой ОС (раздел 12). На macOS это `osascript` из собственного процесса: разрешение Automation не нужно, окно само выходит на передний план (проверено 09.09.2026), режим Focus прячет уведомления, а не окна; перед показом `beep`. До трех вариантов - кнопки `display dialog` (кнопка с именем Cancel и Esc дают `CANCELLED`), больше - список `choose from list`, `--text` добавляет поле. Вопрос регистрируется в `asks.json` и журнале, диалог держит отвязанный процесс `longrun ask _wait Q<n>` (setsid, переживает конец вызова Bash и MCP). Спросивший ждет ответ до `--wait` и возвращает его инлайн; по истечении снимает `claimed`, и процесс диалога сам доставляет ответ через `deliver_to`: сокет сессии (ход начинается сразу, даже посреди хода), иначе inbox, иначе spool. Гонка между ними закрыта общим замком сессии: ответ либо уходит инлайн, либо сообщением, но не пропадает. `--expire` (по умолчанию 360 мин) закрывает диалог сам (`giving up after`, для списка - таймаут процесса).

MCP-сервер `longrun mcp`: JSON-RPC по stdio, одна строка на сообщение, без зависимостей; методы initialize, tools/list, tools/call, ping, остальное - ошибка -32601; вызовы тулов в потоках, чтобы отмена и второй вызов не ждали первого. Сессию находит по pid родителя в `~/.claude/sessions/<pid>.json` (сервер - дочерний процесс `claude`; обход до шести уровней вверх), проект - по индексу сессии, иначе по cwd; без проекта диалог показывается инлайн без поздней доставки. По документации Claude Code: таймаут MCP-тула по умолчанию около 28 часов, idle-таймаут stdio-сервера 30 минут без ответа, вызов дольше 2 минут в интерактивной сессии уходит в фоновую задачу; поэтому `wait_sec` ограничен 1500 с. Во время halt тулы `mcp__longrun__*` не запрещаются.

Переменные окружения для тестов: `LONGRUN_ASK_FIXTURE=<json {"button","text","state"}>` заменяет диалог, `LONGRUN_ASK_FIXTURE_DELAY=<сек>` задерживает ответ, `LONGRUN_NO_UI=1` запрещает настоящие диалоги и уведомления (без фикстуры `ask` отвечает `FAILED`), `LONGRUN_NO_TIMER=1` запрещает трогать launchd, systemd и crontab, `LONGRUN_DESKTOP_DIR=<каталог>` подменяет каталог метаданных приложения.

---

## 12. Платформа: macOS и Linux

Все, кроме четырех вещей, - обычный POSIX-питон без зависимостей, и работает одинаково. Эти четыре решаются в рантайме, один раз, по тому, что на машине реально есть.

**Таймер, который гоняет тик.** Его ставит `install.sh` (`--no-timer` пропускает), и первый `watch add` ставит его, если таймера нет; `longrun watch install` заводит то, что сказано в ключе `watch_timer`, а по умолчанию то, что дает эта ОС:

| ОС | Что создается | Где | Как посмотреть |
|---|---|---|---|
| macOS | агент launchd `com.longrun.watch`, `StartInterval`, `RunAtLoad` | `~/Library/LaunchAgents/com.longrun.watch.plist` | `launchctl print gui/<uid>/com.longrun.watch` |
| Linux с пользовательским systemd | `longrun-watch.service` (`Type=oneshot`) и `longrun-watch.timer` (`OnUnitActiveSec`, `OnBootSec=1min`), включается с `--now` и один раз запускается сразу | `~/.config/systemd/user/` | `systemctl --user list-timers longrun-watch.timer`, `journalctl --user -u longrun-watch.service` |
| Linux без него (контейнер, WSL1, голый `su`) | одна строка в crontab пользователя, помеченная комментарием, чтобы `uninstall` убрал ровно ее и ничего чужого | `crontab -l` | `longrun watch status` |

Любой таймер запускает проверку с почти пустым окружением, поэтому PATH снимается с настоящего шелла в `watch/env.json` при `add` и `install`; на Linux туда же попадают `DISPLAY`, `WAYLAND_DISPLAY`, `XDG_RUNTIME_DIR` и `DBUS_SESSION_BUS_ADDRESS`, чтобы уведомление из тика дошло до рабочего стола. Точность интервала: launchd догоняет пропущенные интервалы на пробуждении, `OnUnitActiveSec` считается по `CLOCK_BOOTTIME` и ведет себя так же, у cron просто пол в одну минуту. Пользовательский таймер systemd работает, только пока у пользователя есть сессия: на сервере без `loginctl enable-linger <user>` он останавливается при выходе, и `watch install` говорит об этом, если linger выключен.

**Диалог за `ask` и `mcp__longrun__ask`.** macOS: `osascript`. Linux: `zenity`, иначе `kdialog`, и только когда выставлен `DISPLAY` или `WAYLAND_DISPLAY`. Ни одна из этих программ не умеет кнопки и поле ввода одновременно, поэтому с `--text` на Linux побеждает поле, а кнопкой в ответе приезжает вариант по умолчанию. zenity раскладывает варианты на `--question` (`--ok-label` - вариант по умолчанию, `--extra-button` - средний), больше трех - на `--list`, и держит `--expire` своим `--timeout`; kdialog использует `--yesno` и `--menu`, а истечение срока у него - это убийство процесса. Машина вообще без диалога - сервер, сессия по ssh, не поставлен zenity - это не аварийное состояние: `ask` возвращает `FAILED Q<n>` с причиной, а скилл велит агенту спросить в чате.

**Уведомление на рабочем столе** - то, чем сообщают о себе `halt`, сработавший watch и MCP-тул `notify`. Оно необязательно на обеих системах, и longrun не зависит ни от одной из этих программ: все, что несет уведомление, и так доезжает до самой сессии через ее сокет или инбокс, поэтому машина, где ничего не установлено, - поддерживаемая конфигурация, и все четыре набора тестов гоняются именно так. Программу задает ключ `notifier`; `auto` берет `terminal-notifier`, если он поставлен, иначе `osascript` на macOS и `notify-send` на линуксовом рабочем столе, иначе не берет ничего - и это нормальное состояние сервера, а не поломка: сообщение все равно доедет до сессии через сокет или инбокс.

terminal-notifier стоит первым из-за ловушки macOS, которую снаружи не видно. `osascript -e 'display notification'` отправляет уведомление от имени Script Editor, а у того нет собственного разрешения на уведомления (нет ключа `auth` в `com.apple.ncprefs`), поэтому macOS кладет уведомление в базу и не рисует ничего: проверено 10.09.2026, когда 25 из 25 уведомлений через osascript лежали в базе Центра уведомлений с `presented = 0`, а уведомления самого приложения Claude показывались нормально. Ошибки при этом не печатается нигде - `osascript` выходит с нулем. У terminal-notifier свой подписанный бандл, поэтому у него своя запись и свое разрешение; после `brew install terminal-notifier` бандл нужно один раз зарегистрировать (`lsregister -f` и один `open`), иначе он отвечает `Could not request notification permission`.

**Уведомление о конце хода.** Приложение Claude шлет его само, но с `silent: true` и `interruptionLevel: "passive"` - а это на macOS значит, что система кладет уведомление в Центр уведомлений, не подсвечивая экран, не проигрывая звук и не рисуя баннер. Всплывает только второй тип, `needs_input` ("Claude is waiting for your input"), он уходит как `active`. Настройки на это нет: `notificationLevels` самого приложения выбирает лишь `off | badge | banner` и на interruption level не влияет. Поэтому громкий вариант шлет `notify_turn_end` из хука `Stop`. Режим `unfocused` спрашивает у Launch Services (`lsappinfo front` - без Automation-разрешения и без запроса, в отличие от пути через `System Events`), какое приложение сейчас впереди, и молчит, пока это Claude. Сессия в терминале не знает, чье окно впереди, поэтому там `unfocused` работает как `always`. Проверено на Claude.app 2.1.260, 10.09.2026.

**Важные сессии.** Обе проверки выше глобальные, и у обеих одна слепая зона: пока вы работаете в одной сессии Claude, приложение в фокусе, поэтому `unfocused` молчит про *все* сессии - включая ту долгую, которую вы как раз ждете. `longrun important` помечает одну сессию исключением: хук `Stop` шлет уведомление независимо от `notify_turn_end` и от того, кто в фокусе, а заголовок начинается со звездочки, чтобы такое уведомление было видно среди остальных. У флага три формы - `on` (пока не снимут), `next` (один конец хода) и `N` (счетчик), - и лежит он в `meta.json` самой сессии, рядом со счетчиками: поэтому переживает компакцию, `/clear` и resume (ключ сессии идет по цепочке resume, а id из CLI - нет). Счетчик тратится внутри того же лока, который пишет мету хода, так что два хука гонкой не потратят его дважды, и каждая отправка пишется в журнал сессии вместе с программой, которая ее доставила, - или с `NOT sent: no notifier here`, если на машине уведомлений нет вовсе.

**Иконка и имя отправителя.** И то и другое берется из бандла, который отправил уведомление, и переопределить их у отдельного уведомления macOS не дает - именно поэтому в terminal-notifier 3.1.0 выключены `-appIcon` и `-sender`. До 0.6.0 longrun собирал собственную копию terminal-notifier.app, чтобы иметь свою иконку; это убрали, потому что подвижных частей вышло больше, чем стоила иконка, и теперь баннер несет имя и иконку самого terminal-notifier.

**Куда ведет клик.** Уведомление longrun несет диплинк той сессии, о которой оно: клик открывает именно ее, а не просто поднимает окно - `claude://code/continue?session=local_<uuid>&source=desktop_action`. Схему `claude` регистрирует само приложение, а параметр проверяет регуляркой `/^local_[A-Za-z0-9-]+/` - это ровно тот id, который longrun и так хранит как `desktop_id`, и та же ссылка, которую приложение строит для собственных уведомлений. Нести цель клика умеет только terminal-notifier (`-open`); у osascript и notify-send такого нет, и ссылка просто отбрасывается. У сессии без записи в приложении (только CLI, или метаданные еще не записаны) ссылки нет, уведомление все равно уходит.

`install.sh` спрашивает, настраивать ли уведомления (`--notify` / `--no-notify` отвечают заранее, а запуск, которому некого спросить, их пропускает); после согласия все это он делает за вас: на macOS ставит terminal-notifier через brew, если его нет, и говорит об этом; `--no-notify` пропускает всю эту часть. Проверка - `longrun notify --test`: он печатает цель клика и шлет пробу, а на macOS пробует вычитать флаг `presented` из `~/Library/Group Containers/group.com.apple.usernoted/db2/db`, а не гадает. `presented=1` - баннер действительно нарисовали; `presented=0` - доставлено и выброшено, и тогда печатается, чем чинить. База живет в режиме WAL, а система чекпойнтит ее лениво, поэтому она копируется вместе с `-wal` и копия открывается на запись, чтобы sqlite проиграл журнал; строки все равно может не оказаться (в таблице лежит то, что сейчас в Центре уведомлений, и система сама выкидывает записи) - тогда команда говорит, что ответа не знает, и просит посмотреть на экран.

Мелочи, решаемые тем же способом: метаданные приложения лежат в `~/Library/Application Support/Claude` на macOS и в `$XDG_CONFIG_HOME/Claude` на Linux (нет приложения вовсе - просто нет заголовков сайдбара, адресация по id сессии продолжает работать); запущенная проверка получает `LANG=en_US.UTF-8` на macOS и `LANG=C.UTF-8` на Linux; в снимок PATH дополнительно заглядывают `/home/linuxbrew/.linuxbrew/bin` и `/usr/local/sbin`.

